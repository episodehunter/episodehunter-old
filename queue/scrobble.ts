const scrobble = {
    sync: {
        watched: {
            show: {
                add: 'scrobble.sync.watched.show.add',
                get: 'scrobble.sync.watched.show.get'
            }
        }
    }
}

export {scrobble};
